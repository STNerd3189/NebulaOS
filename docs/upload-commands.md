# Upload Commands

Run these from the project root once Git is available:

```bash
git init
git branch -M main
git add .
git commit -m "Initial NebulaOS project scaffold"
git remote add origin https://github.com/your-username/nebulaos.git
git push -u origin main
```
